version: "3.8"
services:
  react-app:
    build: 
      context: ..
      dockerfile: docker/Dockerfile
    container_name: react-app-container
    ports:
      - "80:80"
    restart: unless-stopped
    